# Placeholder for Kubernetes service
apiVersion: v1
kind: Service
metadata:
  name: genesis-engine
spec:
  selector:
    app: genesis-engine
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
  type: ClusterIP